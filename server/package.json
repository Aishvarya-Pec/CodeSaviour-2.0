{
  "name": "codesaviour-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for CodeSaviour API",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}